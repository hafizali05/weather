import React, { Component } from "react";
import Search from "../container/search_bar";

export default class App extends Component {
  render() {
    return (
      <div>
        <Search />
      </div>
    );
  }
}
